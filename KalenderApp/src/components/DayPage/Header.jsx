import React from "react";

const Header = (props) => {
  return (
    <header className="header">
      <h1>Task Tracker</h1>
      <button className="btn">Add</button>
    </header>
  );
};

export default Header;
